# Handbag PDP craftsmanship images — September 5, 2026

Twenty-four accepted GPT Image 2 photographs: four material, stitching, finishing and hardware macros for each of the six remaining active handbag families. Vivienne has its own completed set in `../craftsmanship-followup-2026-09-05/`.

Use `delivery-manifest.json` for theme upload. It contains all 24 JPEG paths, stable asset filenames, alt text, headings, color bindings, original provider URLs, source crops and SHA256 values. The accepted square JPEGs are in `web/`; the unchanged 2048px provider PNGs remain in `final/`. Web export only resizes uniformly to 1024×1024 and encodes JPEG. No product pixels were painted, composited or altered after generation.

The selected families and colors are Weekender Army Green, Meridian Brown, Camille Navy, Colette Caramel, Delphine Light Chocolate and Juliette Camel. Map the Weekender set to both active Weekender/Eleanor handles. The five accessory listings and seven draft collection listings are outside this active handbag media scope.

## Provenance and review

- `catalog-media-audit.json` records handles, product IDs, evidence limits and dimension provenance.
- `prompts-and-provenance.json` contains every prompt, input crop box/hash, full source/hash, private-wrapper job receipt and accepted output lineage. `receipts/` contains provider status receipts. Production used the existing `_engine/mcp/ad-engine/engines/kie.py` GPT Image 2 route.
- `source-crops/` contains lossless rectangular crops of the selected references, prepared only as generation inputs. No R40 photography or third-party Juliette reference was supplied to generation.
- `qa.json` records all 24 visual acceptances and output hashes. Each family’s `qa/*-source-generated-crop.jpg` shows original crop, generated photograph and the 341:272 center crop with the theme’s 0.8 saturation preview. That crop/desaturation is QA-only, not baked into delivered images.
- All 24 first submitted outputs passed the selected-view review. The initial unsubmitted Cognac Weekender crop plan is retained only as history; Army Green is the final color. A Colette input crop was widened before generation to keep the gold cap visible.

These are illustrative photographs of finished-product details. They do not document a factory, a manufacturing operation or a certification. Weekender uses actual product-still lineage; Delphine uses its corrected August 30 creative master; Colette and Juliette use their current creative targets. Generated creative targets establish intended appearance, not independently verified manufactured-unit dimensions. No composition, measured scale, hidden construction, capacity or mechanism-function claim is established by these macros.

No Shopify catalog, theme or external-store mutations were performed by this media task. Film generation and storefront integration are handled separately by the coordinating agents. HyperFrames, Remotion and video assembly were not used.
