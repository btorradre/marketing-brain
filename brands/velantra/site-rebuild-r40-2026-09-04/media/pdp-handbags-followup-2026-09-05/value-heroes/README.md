# Value-panel hero delivery

Use the two current entries in `delivery-manifest.json`: `weekender-value-hero-v2.jpg` and `delphine-value-hero.jpg`. The older `weekender-value-hero.jpg` is superseded and must not be selected.

The Weekender v2 uses the real Army Green closed-front photo from August 8. It retains the source-visible horizontal oval with an empty center opening, contoured flap, brown leather and olive woven body. The source-to-output outline comparison is in `weekender-source-v1-v2-review.jpg`; image-landmark measurements are in `weekender-outline-review.json`. Approximate maximum width divided by handle-crown-to-base height is 1.2638 in the source, 1.1026 in rejected v1, and 1.2848 in accepted v2. This is relative photographic geometry, not a physical measurement or confirmation of current-unit size.

The Delphine uses the corrected August 30 Light Chocolate creative master, which is a design reference rather than proof of a manufactured unit. The acceptance scope is source-relative appearance.

Original GPT Image 2 provider PNGs, prompts, source hashes and provider receipts are retained. Web JPGs only receive uniform downscaling to a maximum 1024 pixels and JPEG encoding. No width or height stretching, retouching, recoloring or local background removal is applied. Scope is two value-panel overrides; original product galleries are preserved.
