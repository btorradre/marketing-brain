> Superseded working handoff: the user confirmed CapCut, and all three native revised projects and exports are complete. See `../capcut/manifest.json` and http://localhost:8765/assets/mot-vid-013-revised-r3/review.html. This JSON was not the final production route.

# MOT-VID-013 Hook A — R3 internal-editor draft

Editing is approved. This document was assembled using the documented `@adengine/timeline` package. It validates with 40 continuous picture scenes, 32 approved 1.1× narration segments, 95 exact caption phrases/time windows and a 95.9-second total. All source files are present; no picture source is reused byte-for-byte. The 1 / 2 / 3 setup card precedes the first celery image at frame 1387.

Open/import `MOT-VID-013-HOOK-A-R3.timeline.json` through the current internal editor once its playback/export interface is identified. Asset references point to existing local originals. The event log and source mapping are alongside it. The source CapCut draft has not been modified.

This is not an MP4 export. Before export, visually verify font-size translation, callout position, caption safe zones, source range choices, audio fades/joins, the minor S07 line drift and every rendered cut. Text exposure and source audio edit timing are verified against R2, but presentation parity requires actual playback. Use the authorized editor to render 1080 × 1920 at 30 fps, without changing narration speed.
