# Engine Defaults (tool/engine facts, not laws)

Migrated from Claude Code auto-memory `feedback_*.md` files on 2026-09-03. Standing settings, model ids, parameters and measured engine behaviours. Where two memory files disagree the row says so.

| setting | value | why | source |
|---|---|---|---|
| B-roll i2v engine (VO-style ads) | Google Omni 1.1 Flash via the Gemini Interactions API (curl transport; look up the model id with `client.models.list()` filtered on "omni"; working id in `cashmere-tote/broll/organic-library-2026-09/scripts/omni_animate.py`) | "always use omni 1.1 flash from here on out for broll"; newer, faster, cheaper than the hard-coded `gemini-omni-flash-preview`; do not fall back to older ids to match earlier batches | feedback_omni_11_flash_and_gemini_37_flash (2026-09-02) |
| Video analysis / QA model | `gemini-3.7-flash` for every pass that watches or scores video (reference-TikTok analysis, clip drift QA, keyframe QA); update `animate_omni.py`, crawler `analyze.py`, any new runner | "gemini 3.7 flash for video analysis"; one standing answer | feedback_omni_11_flash_and_gemini_37_flash (2026-09-02) |
| Omni clip spec | 10s 720p i2v with native audio, renders in ~40-60s; 9:16 via prompt footer; keyframe-seeded ("Animate this exact frame") | dramatically cheaper than Seedance; fidelity held on the Margot run | feedback_broll_engine_rule_omni_vs_seedance; feedback_omni_over_seedance_cost |
| Omni drift budget | ~8-9% drift on normal scenes; ONE re-roll; macros/engraving-prone scenes fail once then go to zoompan push-in from the approved still (upscale 3x, zoompan ~1.00→1.11 over 10s; script `brands/velantra/_shared/creative/broll-library-2026-08/kenburns.py`) | extreme macro hardware never survived re-rolls (0/5); metal plates and small props grow garbled text | feedback_omni_macro_mutation (2026-08-02) |
| Omni product beats | never animated in Omni; hero-product beats are Ken Burns crop-pans from the QA-approved keyframe | 5 of 9 jar-centric Omni clips mutated the Motilli jar; 13/14 creator/mechanism beats passed | feedback_broll_engine_rule_omni_vs_seedance (2026-08-17) |
| Omni motion prompt style | affirmative construction locks only; hard negatives go in GPT Image keyframe prompts | dense "never / no / NEVER" stacks return `Input blocked: The prompt could not be processed` | feedback_i2i_anchor_on_the_fitting_not_the_product (2026-08-24) |
| Omni audio input | not available (standalone audio API-gated; video-input workaround intermittently 400s) → Omni is short-ads-only for talking heads | long-form needs an engine that accepts a supplied audio track | feedback_longform_one_take_audio_law (2026-08-06) |
| Seedance version for dialogue/lipsync ads | Seedance 2.5 via kie; check `kie_balance` first, auto top-up broken | only earns its cost for native spoken dialogue, lipsync, or a choreographed 30s multi-beat pass | feedback_broll_engine_rule_omni_vs_seedance (2026-08-17) |
| Seedance 2.0 mode | `--mode std`, 9:16, 720p; `fast` is BANNED; never downgrade to fit a credit balance, stop and ask for a top-up | fast mode produced warped hands and face melt | feedback_ugc_dialogue_naturalness (2026-07-06) |
| Seedance 2.0 cost | ~41cr/s at 720p on kie (22s ad ≈ 900cr for clips; regens double it) | why Omni is the default | feedback_omni_over_seedance_cost (2026-07-27) |
| Seedance 2.5 max clip | 30s single pass | "extend the beat (Seedance 2.5 goes to 30s)" | feedback_spoken_lines_complete_sentences (2026-09-02) |
| Seedance audio input | accepts `reference_audio_urls` on kie (qualifies for the one-take audio pipeline) | engine must accept supplied audio for long-form | feedback_longform_one_take_audio_law (2026-08-06) |
| Seedance reference faces | ByteDance policy: 2.5/2.0 do not accept real human faces in reference images; kie has been passing Pinterest refs regardless; first thing to check if a run rejects/ignores the creator ref; fallback = reuse a generated creator frame as the identity reference | watch item | feedback_seedance_ugc_pinterest_creators (2026-08-07) |
| Seedance native text | cannot spell "Weekender" (Weekaner/Weekaver 2/2); strip captions from the prompt and overlay via Pillow PNG; native two-line captions must not use periods as line delimiters; pin size/position explicitly | native text drifts in size/placement per segment | feedback_weekender_product_truth_skill (2026-07-10) |
| Seedance UGC prompt format | DENSE timestamped UGC Director format (`seedanceugcdirector`): 3×5s blocks per 15s segment locking Camera / verbatim Creator block / both hands / Face / In frame / NOT in frame / Light / Background + Audio line; 30-45 words per 15s; @Image refs numbered by attachment order; no hyphens, no em dashes, numerals, never "cinematic" | "density is survival"; Seedance invents garbage for anything undescribed (supersedes the short-prompt law) | feedback_ugc_dialogue_naturalness (2026-07-06 PM) |
| Omni UGC segment format | SIMPLE 4-part prompt blocks (movement line / voice line / dialogue / fixed footer); 8-10s segments, ≤34 words, sentence boundaries; creator wired via image input, no @Image tags | dense JSON format RETIRED for the Omni era ("way too complicated, takes forever") | feedback_segment_brief_standard (2026-07-08) |
| i2v clip duration (concept runs) | fixed 6 seconds per clip, any engine; edit cadence 3-4s | trim headroom; clips never stretch | feedback_fixed_6s_video_segments (2026-07-18) |
| Kling segment length | max 5s (never 10s); start=end frame lock `image_urls: [keyframe, keyframe]`; prompts ≤2 sentences; shared keyframe across continuation segments | Kling is trained heaviest on 5s; 10s drifts and hallucinates | feedback_animated_video_hardwon_rules |
| Kling organic cues | append: "subtle inconsistent handheld micro jitter"; "slight rolling shutter wobble during motion"; "eyes briefly lose focus, then re-lock onto camera"; "background movement continues even when subject is still" (skip the eye cue with no human); centralize as `ORGANIC_CUES` | kills the locked-off "AI video" tell | feedback_kling_prompting |
| Kling 3 cost | ≈90cr/sec real on kie; 6s ≈ 540cr/clip; `kling3_0` 5s std sound-off = 7.5 credits on Higgsfield Ultra | budgeting | feedback_fixed_6s_video_segments; feedback_hailuo_push_in_prior |
| Kling used window | ship the early window (usually 1-1.5s of a 6s clip); sample the frame at the trim point | mutations accumulate late | feedback_kling_bag_mutation_signature (2026-07-24) |
| Animated ads engine | Nano Banana 2 i2i on every reference keyframe → Veo 3.1 image-to-video with the i2i frame as START FRAME (native audio); never Kling, never text-to-video alone. CONFLICTS with feedback_animated_video_hardwon_rules (Kling 5s segments + ElevenLabs clone) and the animated-video skill (Kling 3.0 native audio) | Veo 3.1 generates audio natively, Kling does not | feedback_animated_replicator |
| Kling multi-segment dialogue voice | ElevenLabs cloned voice, audio-swapped onto clips; Kling native TTS (`sound=True`) only for single clips | native TTS drifts accent across clips | feedback_animated_video_hardwon_rules |
| Hailuo (minimax-2.3) | hard push-in prior, un-promptable; 6s 1080p = 10 credits, `-fast` 768p = 4; output 1080x1930 (crop to 1920), no audio track; take first 30-50% of the clip | design shots so the push helps (subject walking away, start wide) | feedback_hailuo_push_in_prior (2026-08-11) |
| Higgsfield Ultra economics | GPT Image 2 ≈ 4 credits per 1k 9:16 image (roll extras); concurrency ~8-9 jobs then 429 `rate_limit_reached`; `submission_failed` asking for a preset → resubmit with `declined_preset_id` | staggering and budgeting | feedback_hailuo_push_in_prior (2026-08-11) |
| Creator identity lock | Higgsfield Reference Element (`show_reference_elements action:create` from ONE casting still) embedded as `<<<element_id>>>` in EVERY prompt incl. hands-only inserts; works with GPT Image 2 / Nano Banana / Seedream / Kling / Seedance; Soul training (5-20 photos, ~10 min, Soul V2/Cinema only) is the heavier alternative | identity held across 16 frames with zero drift | feedback_one_creator_element_lock (2026-08-11) |
| i2i model | GPT Image 2 (`gpt_image_2` on Higgsfield CLI) for ALL i2i, never Nano Banana | Nano Banana 2 reverts to generic jar/bottle priors; gpt_image_2 rendered a legible label | feedback_product_shots_i2i (2026-07-06) |
| Open-bag Weekender frames | GPT Image 2 i2i, quality high, 2k, 9:16, anchored on the open-interior reference ONLY (real 8/08 open stills); never Seedance ref-mode | Seedance split the flap ~20x in one day even with the block pasted | feedback_weekender_product_truth_skill (2026-07-10, 2026-08-08) |
| Static ads | `gpt_image_2`, `quality: "high"`, `use_unlim: false`, via Higgsfield `generate_image_batch`; product image as `medias` reference (role `image`); no native 4:5 (API coerces to 3:4) → generate 3:4, pad sides to 4:5, resize 1080x1350; a bone hex needs "NOT yellow, NOT butter, NOT beige-gold, NOT tan"; describe feces as "mud" or "modelling clay" to clear the nsfw filter | GPT Image 2 renders long ad copy accurately at high quality | feedback_static_ads_oneshot_gpt_image (2026-08-16) |
| GPT Image 2 emboss text | renders "VELANTRA" emboss in-keyframe reliably (1 garble in 3); QA-gate spelling | keyframe-native brand text is viable | feedback_velantra_brand_films_no_founder (2026-07-23) |
| Product reference (Motilli) | multiple paths recorded: `brands/motilli/creative/product-images/updated/product img 1.webp` (clean packshot), `brands/motilli/brand/brandkit/assets/product_hero.png` (marketing card), `brands/motilli/motilli pdp/images/hero-01.png`, `brands/motilli/brand/website-assets/motilli product reference.png` | canonical path drifted across vault reorgs; verify before wiring | feedback_product_shots_i2i; feedback_motilli_gummy_color; feedback_animated_replicator |
| WebP references | convert to PNG first; PIL + `google.genai.types.Part.from_image` silently drops WebP | silent failure | feedback_product_shots_i2i |
| Nano Banana handwriting | flat-table handwriting renders upside-down/mirrored; fix by same-side over-the-shoulder camera; vertical surfaces are fine | model resolves orientation from the implied viewer of the surface | feedback_native_image_handwriting_orientation (2026-08-26) |
| Editor avatar route | Pinterest search link → one calm still → Nano Banana Pro i2i (chest-up selfie angle, mouth mid-sentence, greenscreen `#00B140` when keyed) → i2v/HeyGen with the VO; one creator per series | supersedes from-scratch text-to-image | feedback_editor_brief_no_file_paths (2026-07-28) |
| Editor b-roll route | GPT Image 2 i2i keyframe (seeded from live product reference) → QA → Google Omni motion-only prompt | never one combined image+motion prompt; never generate the keyframe in Omni | feedback_editor_brief_no_file_paths (2026-07-28) |
| Creator reference source (Seedance UGC) | Pinterest image Brooks supplies per run; 5-avatar roster only when he names one; save the image into the concept folder. MEMORY.md index marks "Pinterest refs RETIRED" (9/1) but the file is not updated | Brooks hand-picks each look | feedback_seedance_ugc_pinterest_creators (2026-08-05) |
| Casting variety | hair, age and styling only (e.g. silver-blonde/cream knit 58, brown-going-grey/rust knit 53, silver bob/black turtleneck 62) | per the 2026-08-31 casting directive | feedback_creator_casting_white_women_only |
| ElevenLabs VO model + settings | `model_id: eleven_v3`, `stability: 0.0` (Creative; v3 accepts only 0.0/0.5/1.0), `similarity_boost: 0.85`, `use_speaker_boost: true`; plain script (audio tags stiffened pacing on library voices; light `[casual]`/`[chuckles]` tags OK on a clone but gate every take); `/v1/text-to-speech/{id}/with-timestamps` for word alignment; no speed control → `atempo` in ffmpeg then re-run the STT gate | multilingual_v2 at defaults scored 2.5/10 vs 9.5/10 for v3 Creative | feedback_elevenlabs_v3_creative_vo (2026-08-05); feedback_elevenlabs_v3_needs_ivc_not_pvc (2026-09-02) |
| ElevenLabs voice type | Instant Voice Clone of a real creator (yt-dlp TikTok → mp3 mono 44.1k → `highpass=f=80,loudnorm=I=-18` → Scribe diarize check → `eleven_clone_voice` → 3 takes → STT gate); check `GET /v1/voices/{id}` `high_quality_base_model_ids` for eleven_v3; never a `professional` PVC on v3; premade voices read like narrators | PVCs run off the untuned base and read flat | feedback_elevenlabs_v3_needs_ivc_not_pvc (2026-09-02) |
| Registered clone | `gringo-tiktok-male` = `hnRXaWYxr5tqZBrH55Cp`, source `concepts/9:2:26 - erling haaland/voice/gringo_ref_norm.mp3` | reuse | feedback_elevenlabs_v3_needs_ivc_not_pvc (2026-09-02) |
| ElevenLabs "Woman Over 40" voice | `NBIPq5xdnIg9kaBH5Ape`, eleven_v3 Creative; HeyGen default voice | Brooks's standing HeyGen default | feedback_heygen_avatar_v_woman_over_40_default (2026-08-31) |
| v3 text cap | 5,000 characters (400 `text_too_long`); split a ~5-min VSL at a paragraph boundary into two takes, same voice/settings, stitch with ~0.35s `apad` + `concat` | validated on MOT-Bloom-LaxTrap-VSL (206s + 193s) | feedback_elevenlabs_v3_creative_vo (2026-08-09) |
| v3 minimum span | unstable under ~250 characters; generate the whole script and slice on word timestamps | prosody comes from the surrounding lines | feedback_elevenlabs_v3_creative_vo (2026-08-05) |
| Clone source | clone from every shipped segment (two ~14s takes beat one ~8s) | better match | feedback_elevenlabs_v3_creative_vo (2026-08-05) |
| Room match for spliced VO | `aecho=0.9:0.75:14\|27\|41:0.10\|0.06\|0.035` + room-tone bed from the on-camera segment's pre-speech head at -24dB | judges flagged the acoustic change, not the voice | feedback_elevenlabs_v3_creative_vo (2026-08-05) |
| "Velantra" TTS spelling | CONFLICT: feedback_elevenlabs_v3_creative_vo (2026-08-09) says `Vell-Ahn-Trah`, `Vel Lantra`, `Vay-lahn-truh` land and `Vehlantra` failed; feedback_tts_respell_hyphens_cause_pauses (2026-08-31, later) says hyphens insert audible pauses and plain `Vehlantra` (0.96s / 0.08s gap) and `Vivian` fixed it. Later ruling: plain unhyphenated spellings, mapped back in the caption respell map | only the "Vel" vs "Vol" onset is real signal; STT a/e is jitter; Scribe cannot judge the Vel syllable (word-presence gate only) | feedback_tts_respell_hyphens_cause_pauses; feedback_elevenlabs_v3_creative_vo |
| "Motilli" / "getmotilli.com" TTS | no respelling needed | bias-prompted STT read-back clean | feedback_elevenlabs_v3_creative_vo (2026-08-09) |
| TTS tough words | phonetic spelling ("constipation" → `con-stuh-pay-shun`) in Kling + ElevenLabs dialogue; ellipses may cause ElevenLabs to trail off and drop final words, check Whisper. Note the later hyphen-pause finding on v3 | dropped final words | feedback_animated_video_hardwon_rules |
| VO output shape | one continuous full-length track (`pipeline.py generate --segment-by none` → `voiceover.mp3`) | Brooks drops one file on the timeline | feedback_vo_single_seamless_cut |
| Long-form audio pipeline | render one VO → split THAT audio at sentence boundaries (`_engine/tools/chunk_vo_beats.py`, `trim_deadspots.py`, `pick_take.py`, `scan_consistency.py`, `ad_transcript.py`) → lipsync a clip per chunk → lay the original VO back, discard clip audio; seed every segment from the same avatar keyframe (chained seeding: background similarity 0.53 declining vs 0.86 flat) | prosody restart at every seam is undetectable by metrics | feedback_longform_one_take_audio_law (2026-08-06) |
| HeyGen engine | Avatar V = Avatar 5 = `engine: {"type": "avatar_v"}`; only accepted shape: `type:"avatar"` + `avatar_id:"<created look id>"` + `audio_asset_id` + `engine`; image-to-voice (`type:"image"`) rejects `engine` (`Extra inputs are not permitted`); create the avatar first: upload → `POST /v3/avatars {type:"photo", file:{type:"asset_id"}}` → poll until `completed` → `POST /v3/videos`; valid `type` tags: avatar, image, cinematic_avatar, studio | API-proven 2026-08-31 | feedback_heygen_avatar_v_woman_over_40_default |
| HeyGen engine audit | `GET /v3/videos/{id}` returns `engine: null`; log the submit payload; verify motion by frame-to-frame delta in the head region (healthy Avatar V ≈ 7-10 on 0-255; near zero = frozen face) | cannot audit after the fact | feedback_heygen_avatar_v_woman_over_40_default (2026-08-31) |
| HeyGen source framing | generate creator keyframes with hands and upper body in frame | Avatar V's advantage is hand/gesture motion | feedback_heygen_avatar_v_woman_over_40_default (2026-08-31) |
| HeyGen re-render guard | `render_heygen.py` ffprobes the VO and asserts duration against `VO_EXPECT`; clear `video_id` from `_state.json`, delete `*-green.mp4` / `*-alpha.webm` before re-rendering; re-measure PiP geometry on new plates (head width 501 vs 492px moved the solve 13px) | plates silently lipsync to old audio | feedback_heygen_relipsync_on_every_vo_change (2026-08-31) |
| Chromakey | `chromakey` similarity 0.09-0.12 gives a clean binary alpha; 0.16 collapses the subject (~110 alpha); sweep by reading the alpha plane (`format=yuva420p,chromakey=<hex>:<sim>:<blend>,alphaextract`); re-sample the green per clip (DC plate `0x00882F`/0.08 → after fal lipsync `0x007A28`/0.11/0.02) | cliff, not gradient | feedback_chromakey_despill_neutral_garment (2026-08-10) |
| Despill | OFF by default; `despill=type=green:mix=0.5` turns a cream sweater pink (G 188→152); measured fringe was 0.16% of subject pixels | for subjects near a hot green wall only | feedback_chromakey_despill_neutral_garment (2026-08-10) |
| Greenscreen PiP geometry | head width 0.16-0.23 of frame width (use ~0.20); head top y ≈ 1220-1340 of 1920; body runs ~60px past the bottom edge and ~65-75px past the near side edge (negative overlay x/y); `pip_geometry.py` in `brands/velantra/products/vivienne/concepts/8-31-26-celebrity-quiet-luxury-gs/`; caption cards must clear the head top | "PiP = 32% of frame width" only works for tight head-and-shoulders plates | feedback_greenscreen_pip_edge_cut (2026-08-31) |
| Greenscreen VO spec (Velantra) | creator bottom-left ~25% frame, new visual every 3.5s, word-synced captions, PDP screen rec as the final beat; 85-95 words / 28-30s / ~185 wpm | greenscreen v3 exemplar | feedback_velantra_ad_script_formula (2026-08-21) |
| Caption style on bright footage | fontWeight 800, 54px, white `#FFFFFF`, black stroke 3 at 0.95 opacity, shadow strength 65 (dark-halo doctrine) | default thin white with no stroke disappears on airport windows / cream canvas | feedback_chatcut_captions_asr_audit (2026-08-21) |
| Caption text + timing | text from the strategist script (correct spelling), timings from Whisper re-transcription of the final audio, minimum 0.5s per card | ASR text is not the script | feedback_animated_video_hardwon_rules |
| ChatCut | DEPRECATED (2026-08-31); uploader normalizes to 30fps and `manage_timelines create` makes 30fps timelines, so create projects at fps 30; `update` cannot change fps | history only; port laws to CapCut | feedback_chatcut_captions_asr_audit; feedback_capcut_over_chatcut |
| Editing lane | CapCut bridge kit `_engine/mcp/capcut-kit/` (draft JSON + AX-tree automation); deliverables = CapCut drafts | Brooks's daily editor | feedback_capcut_over_chatcut (2026-08-31) |
| HyperFrames / Remotion | HyperFrames BANNED (skills remain installed, treat as inert); Remotion install killed ("forget remotion") | no code-rendered graphics lane | feedback_hyperframes_banned_capcut_lane (2026-09-02); feedback_capcut_over_chatcut |
| Assembly | one locked 24fps CFR, one encode pass; hard cuts (concat without xfade) between generated segments | mixed fps hitches at every join; crossfades ghost characters | feedback_no_kenburns_fill_use_real_footage (2026-08-22); feedback_animated_video_hardwon_rules |
| Stabilization | vidstab (in `/opt/homebrew/bin/ffmpeg`) or OpenCV; not `deshake` | deshake only kills micro-jitter | feedback_animated_video_hardwon_rules |
| ffmpeg drawtext | local build has NO drawtext → all type via PIL overlays, captions composited per segment (never chained enable-overlays on looped stills: runaway encode) | build limitation | feedback_velantra_brand_films_no_founder (2026-07-23) |
| Still-vs-clip sharpness | stills measure 3-11x sharper than Seedance clips (Laplacian 898 vs 79) | intercut stills read as artifacting | feedback_no_kenburns_fill_use_real_footage (2026-08-22) |
| Generated video QA rate | 3-4fps on the used window; never 1fps or 3-sample scans | artifacts live between the seconds | feedback_seedance_no_invented_support_surface (2026-08-04); feedback_hailuo_push_in_prior |
| Mid-clip mutation window (Seedance) | mutations cluster at ~2-2.5s → dirty-clip trims ≤2.0s (trimcap+seek in stitch) | keep the clean head | feedback_velantra_brand_films_no_founder (2026-07-23) |
| kie behaviour | errors on a large fraction of calls; fire sequentially with a retry loop; failed tasks are not charged; Seedance pre-auth rejects waves >3 at low balance even with auto top-up (WAVE=3); `orchestrate.py upload()` caches kie URLs by file PATH, pop `st["uploads"]` before re-running videos on a regenerated keyframe | bursting makes errors worse; stale cached images animate silently | feedback_no_3d_render_look (2026-07-29); feedback_velantra_brand_films_no_founder (2026-07-23) |
| Brand-film production spend | ≈12,000+ kie credits for 42 Seedance clips + 60+ images (two films × male/female VO) | budgeting reference | feedback_velantra_brand_films_no_founder (2026-07-23) |
| TikTok b-roll crawler | `search_terms` = 1-4 word nouns; analyze.py gate has THE VISUAL SUBJECT TEST (talking heads / faces / displaying-an-object / skits = conf <20); deliver trimmed best segments `--pad 1.5`; raw in `candidates/`; ~30-40 clips for a 4-min VSL; Gemini model per the 3.7 Flash default | topic-matches passed the old gate | feedback_broll_gate_visual_subject_test (2026-08-17); feedback_broll_sourcing_loom_sop (2026-08-18) |
| Canonical character keyframe (animated) | generate shot 1 once; regen subsequent keyframes from it at temperature 0.28-0.35 with explicit locks (hair, lapel pin, 5-finger hands); pure-i2i product reveals with "DO NOT invent fine print" | character and label drift | feedback_animated_video_hardwon_rules |
| Concept code convention (animated) | `ANGLE-ANIM-##`; archive every iteration `_vN`, never delete; artifact library `~/Documents/marketing brain/b-roll/motilli/CONF-ANIM-01/` | Brooks asks to compare versions | feedback_animated_video_hardwon_rules |
| Ad naming | `<AVATAR>_<ANGLE-ID>_<HOOK-N>_<FORMAT>` (e.g. `MENOSKIN_MOT-A-014_H2_UGC`) | rank by angle and by hook | feedback_angle_definition_canonical (2026-08-17) |
| Video brief ID / publish | `<BRAND>-VID-<NNN>`; `publish_brief.py --brand <Brand> --next-id / --file / --list`; Notion tree Video Editing `32cc96bf-9982-80a8-96fd-c88ffae8dacc` → `<Brand> Concepts`; token `MOTILLI_NOTION_TOKEN`; SOP `_engine/sops/Video-Brief-Format-SOP.md`; format 1080x1920 30fps 9:16 | house brief format | feedback_video_brief_format_canonical (2026-08-21) |
| Velantra editor briefs (Notion) | Briefs page `388eca1af5bf80de84e3d9941c97c097`; product refs deep-linked `?variant=<id>` from the Shopify Admin API; Pinterest search `https://www.pinterest.com/search/pins/?q=<query>` | editor-facing | feedback_editor_brief_no_file_paths |
| Avatar/b-roll production SOP | `_engine/sops/Velantra-Avatar-BRoll-Production-SOP.md` (§3/§3b photoreal process, §4 Ken Burns fallback); verbatim photoreal block in velantra-weekender skill | process lives once | feedback_velantra_brief_brevity_sop_split; feedback_no_3d_render_look |
| Segment brief SOP | `_engine/sops/Segment-Brief-SOP.md` (8 sections); reference `brands/motilli/creative/MOT-ELLEN-RUPTURE-UGC-01/`; ai-copy-blacklist at `_engine/copywriting/updated skills/ai-copy-blacklist.md` | non-Velantra concept runs | feedback_segment_brief_standard |
| Naturalizer | `long-form-copy/SKILL.md` §XX-B + `script-naturalizer.md` (CONFIG, LOCK vs REWRITE, 10 tells, density table by runtime) | mandatory spoken pass | feedback_script_naturalizer_layer (2026-08-04) |
| Hook bank | `_engine/swipe-library/hook-bank/HOOK-BANK.md` organized by awareness level | pull hooks from it | feedback_soft_closes_hook_bank (2026-08-19) |
| Angle schema | `.claude/skills/direct-response-os/modules/angle-schema.md` (canonical); `_engine/sops/Angle-Extraction-From-Sub-Avatars-Prompt.md` subordinate | angle = problem | feedback_angle_definition_canonical |
| Verified-buyer copy guards | Vivienne `_production/check_copy.py`; Weekender `check_quotes.py` | machine-enforce quote rules | feedback_verified_buyer_statics_are_approved (2026-08-27) |
| Scrim compositor | `solve_local_scrim` / `local_scrim` in the Vivienne static run's `compose.py` (the Delphine `compose.py` still carries the full-width version) | local feathered box | feedback_scrim_measure_type_span_not_frame (2026-08-27) |
| Open-flap closure block | working `OPEN_CLOSURE` block in the Vivienne static run's `gen_plates.py`; port to Weekender and Straw Tote | open-state hardware | feedback_open_flap_needs_own_closure_block (2026-08-27) |
| Weekender real references | `products/weekender/product-references/real-product-2026-08-08/`: open-bag anchors `LC-open-flap-inner-face-interior.jpg` + `LC-open-interior-slip-pocket.jpg`; macro `LC-macro-turnlock-flap.jpg`; closed `LC-closed-front-unfastened.jpg`; validated fix `_build/gen_wk_fix.py` in the EUROFALL concept | supreme i2i anchors + QA truth, never ad frames | feedback_weekender_no_real_iphone_stills_in_ads (2026-09-02); feedback_i2i_anchor_on_the_fitting_not_the_product |
| Straw Tote references | closed hero `caramel 1.png` for packing shots; `straw birkin opened.png` NOT wired for packing (flap flipped back) | wrong geometry attractor | feedback_flap_mechanism_no_exceptions (2026-07-24) |
| Boat Tote references | `brands/velantra/products/boat-tote/product-references/boat tote/colors/<name>/07-13`; root flats = retired design | live product truth | feedback_boattote_colorway_truth (2026-07-24) |
| Wend product art | `brands/wend/creative/pdp-sections-2026-08-15/` + `brand/label-2026-08-15/`; never `brand/concepts-2026-08-15/` | packaging truth | feedback_wend_packaging_truth (2026-08-16) |
| Shopify product duplication | `productCreate` rebuild (DRAFT, no inventory quantities), never `productDuplicate`; curl-backed GraphQL (urllib hangs) | Brooks's rule | feedback_manual_product_duplication (2026-07-17) |
| PDP verification surfaces | theme template via `shop.py theme-get velantra templates/product.<slug>.json` and product body via `descriptionHtml` / `seo` / `metafields`; patch live via `shop.theme_put`; re-sync body with `productUpdate` + `spec["description_html"]` | `--push` reverts local edits; `--media-only` never rewrites the body | feedback_build_pdp_push_reverts_edits (2026-08-22/23) |
| Pre-order variant marker | Shopify `inventoryPolicy: CONTINUE` with qty 0 tells the customer nothing; scoped notice in the description required | chargebacks / WISMO | feedback_preorder_pdp_must_state_ship_date (2026-08-09) |
| Finished-ad location | `brands/<brand>/products/<product>/concepts/M:D:YY - <name>/`; working dirs stay in `brands/<brand>/videos/` | Brooks organizes by product | feedback_finished_ads_to_product_concepts |
