from pathlib import Path
import json
O=Path(__file__).resolve().parent;ready=json.loads((O/'presenter-ready.json').read_text());speech=json.loads((O/'final-speech-ranges.json').read_text());total=speech['frames'];assert ready['source_frames']>=speech['source_frames'],'Provider is short';D=O/'deliverables';D.mkdir(exist_ok=True)
code='''local r=fu:GetResolve();local pm=r:GetProjectManager();local p=pm:GetCurrentProject();assert(p:GetName()=='MOT-UGC-YAPPER-01 v7 V3 20260910');assert(not p:IsRenderingInProgress());local edit=nil
for i=1,p:GetTimelineCount() do local t=p:GetTimelineByIndex(i);assert(t:GetName()~='v7 FINAL - V3 Creative Avatar V','Final exists; inspect instead of rerunning');if t:GetName()=='v7 V3 Creative - final pacing edit' then edit=t end end;assert(edit);p:SetCurrentTimeline(edit);local t=edit:DuplicateTimeline('v7 FINAL - V3 Creative Avatar V');assert(t);p:SetCurrentTimeline(t);assert(#t:GetItemListInTrack('video',1)==0);local mp=p:GetMediaPool();local item=mp:ImportMedia({__VIDEO__})[1];assert(item);local ranges=__RANGES__;for _,s in ipairs(ranges) do local clip=mp:AppendToTimeline({{mediaPoolItem=item,startFrame=s[1],endFrame=s[2],recordFrame=s[3],trackIndex=1,mediaType=1}})[1];assert(clip);assert(clip:GetStart()==s[3] and clip:GetEnd()==s[4]) end;assert(t:GetEndFrame()==__TOTAL__);assert(#t:GetItemListInTrack('video',2)==20);assert(#t:GetItemListInTrack('video',3)==227);assert(#t:GetItemListInTrack('audio',1)==16);t:DeleteMarkerAtFrame(0);t:SetTrackName('video',1,'Fresh HeyGen Avatar V - V3 Creative');t:AddMarker(0,'Green','V7 V3 Creative - speech tightened','83 pause cuts before Avatar V generation plus15 linked final refinements. No silent end hold. Captions and B-roll aligned to the new master.',1);assert(pm:SaveProject());print('V7_FINAL_ASSEMBLED',t:GetName(),t:GetEndFrame());print('DRP',pm:ExportProject(p:GetName(),__DRP__));print('DRT',t:Export(__DRT__,r.EXPORT_DRT,r.EXPORT_NONE))
p:SetCurrentRenderMode(1);assert(p:SetRenderSettings({ExportVideo=true,ExportAudio=true}));assert(p:SetCurrentRenderFormatAndCodec('mp4','H264'));assert(p:SetRenderSettings({TargetDir=__OUT__,CustomName='Motilli-Unbranded-VSL-v7',SelectAllFrames=true,FormatWidth=1080,FormatHeight=1920,FrameRate=30,ExportVideo=true,ExportAudio=true,AudioCodec='aac',AudioSampleRate=48000,VideoQuality=16000,NetworkOptimization=true}));local id=p:AddRenderJob();assert(id);print('V7_FINAL_RENDER_JOB',id);print('V7_FINAL_RENDER_STARTED',p:StartRendering(id));pm:SaveProject()
'''
for k,v in {'__RANGES__':'{'+','.join('{'+','.join(str(r[k]) for k in ['source_start','source_end','record_start','record_end'])+'}' for r in speech['ranges'])+'}', '__VIDEO__':json.dumps(ready['video']),'__TOTAL__':str(total),'__DRP__':json.dumps(str(D/'MOT-UGC-YAPPER-01-v7-FINAL.drp')),'__DRT__':json.dumps(str(D/'MOT-UGC-YAPPER-01-v7-FINAL.drt')),'__OUT__':json.dumps(str(D))}.items():code=code.replace(k,v)
(O/'assemble-final.lua').write_text(code);print('Final assembly ready')
