[project]
name = "adengine"
version = "0.1.0"
requires-python = ">=3.12"
description = "Hosted ad engine: MCP servers, provider adapters, workers"

[tool.setuptools.packages.find]
where = ["."]
include = ["adengine*"]
